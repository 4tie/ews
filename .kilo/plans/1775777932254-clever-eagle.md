# Plan: Implement AI Two-Mode Output System into T:\Optimizer

## Goal
Add AI features with two-mode output enforcement to T:\Optimizer, following SameGrossNetframework's structure if it's good, or using a better approach if available.

## Analysis of T:\Optimizer
- FastAPI-based web app (like SameGrossNetframework)
- Has routers: backtest, optimizer, settings, web_ui_routes
- Services layer for business logic
- No existing AI integration

## Decision: Use SameGrossNetframework's Approach
The two-mode output system from SameGrossNetframework is well-designed and functional. Will implement the same structure in T:\Optimizer.

## Files to Create/Modify

### New Directory Structure
```
T:\Optimizer\app\ai\
├── __init__.py
├── output_format.py      # Two-mode enforcement (copy from SameGrossNetframework)
├── prompts/
│   ├── __init__.py
│   └── trading.py        # System prompts (copy from SameGrossNetframework)
├── models/
│   ├── __init__.py
│   ├── provider_dispatch.py
│   ├── openrouter_client.py
│   ├── ollama_client.py
│   ├── registry.py
│   └── model_routing_policy.py
├── pipelines/
│   ├── __init__.py
│   ├── orchestrator.py
│   └── classifier.py
├── memory/
│   ├── __init__.py
│   └── threads.py
└── context_builder.py
```

### Modified Files

1. **T:\Optimizer\requirements.txt** - Add AI dependencies:
   ```
   openai
   httpx
   aiohttp
   ```

2. **T:\Optimizer\app\main.py** - Register AI routes

3. **T:\Optimizer\app\routers\** - Add new AI endpoints:
   - `ai_chat.py` - Chat endpoints
   - `evolution.py` - Strategy evolution endpoints

4. **T:\Optimizer\app\services\** - Add AI services:
   - `ai_chat/loop_service.py` - AI loop orchestration
   - `ai_chat/apply_code_service.py` - Code application
   - `results/strategy_intelligence_service.py` - Analysis
   - `results/strategy_intelligence_apply_service.py` - Apply suggestions

## Implementation Order

### Phase 1: Core Infrastructure
1. Create `app/ai/` directory structure
2. Copy `output_format.py` from SameGrossNetframework
3. Create `app/ai/prompts/trading.py` with system prompts
4. Add AI dependencies to requirements.txt

### Phase 2: AI Models Layer
5. Copy/create `app/ai/models/` - provider dispatch, clients, registry
6. Copy/create `app/ai/pipelines/` - orchestrator, classifier

### Phase 3: AI Services
7. Copy `app/services/ai_chat/apply_code_service.py` (modified for two-mode)
8. Copy `app/services/ai_chat/loop_service.py`
9. Copy `app/services/results/strategy_intelligence_service.py`
10. Copy `app/services/results/strategy_intelligence_apply_service.py`

### Phase 4: Router Integration
11. Add AI routes to `app/routers/`
12. Wire into main.py

### Phase 5: Testing & Validation
13. Verify two-mode output enforcement works
14. Test parameter-only mode
15. Test code patch mode
16. Verify non-applicable suggestions are flagged

## Dependencies to Add
```
openai>=1.0.0
httpx>=0.27.0
aiohttp>=3.9.0
```

## Key Features from SameGrossNetframework to Preserve
1. Two-mode output enforcement (Mode A: params, Mode B: code patch)
2. Output format validation
3. Non-applicable suggestion detection
4. Strategy mutation with unified diff format
5. Parameter recommendations

## Notes
- Will adapt imports to work with T:\Optimizer's service layer
- May need to simplify some dependencies that rely on SameGrossNetframework-specific services
- Will keep the core output_format.py logic intact